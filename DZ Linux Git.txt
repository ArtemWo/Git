что такое ветка в GIT;
это ответвленная часть проекта над которым я работаю, 
ветка нужно для того чтобы разрабочики не мешали друг другу.

какое отношение имеют ssh-ключи к GITHUB;
без порольная аутодификация (выгружать файлы в аккаунт без авторизацией)
что позволяет делать сервис GitHub Pages.

что позволяет делать сервис GitHub Pages:
для публикации сайтов того сайта над которым ведем работу...

создание, удаление веток:
git branch NAME -- для создания новой ветки 
git branch -D  --удаляю ветку локально (на ПК)
git push origin -d ветка -- удаление ветки (дистанционно т.е на  GitHub)

переключение между ветками:
переключение между ветками --- git checkout ветка

загрузка и удаление веток на GitHub:
git push -u origin ветка ---загрузка
git push origin -d ветка -- удаление ветки (дистанционно т.е на  GitHub)

Коротко опишите, каким образом происходит публикация статического веб-сайта 
с помощью GitHub Pages:
Создаем репозиторий - Setting - Pages - в меню Branch выбераем ветку -Save
